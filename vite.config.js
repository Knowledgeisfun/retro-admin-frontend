import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    host: true,
    allowedHosts: true // This tells Vite to accept any incoming tunnel host (like localtunnel or ngrok)
  }
})